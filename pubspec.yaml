name: irblaster_controller
description: Create custom remote using hex codes or raw signals.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.1.2.12+7
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 3.3.4+42

environment:
  sdk: '>=3.2.3 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  dynamic_color: ^1.8.1
  file_picker: ^10.3.8
  file_selector: ^1.1.0
  font_awesome_flutter: ^11.0.0
  home_widget: ^0.8.1
  http: ^1.5.0
  image_picker: ^1.2.1
  image_picker_android: ^0.8.13+13
  media_store_plus: ^0.1.3
  package_info_plus: ^9.0.0
  path: ^1.9.1
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  reorderable_grid_view: ^2.2.8
  shared_preferences: ^2.5.4
  sqflite: ^2.4.2
  url_launcher: ^6.3.2
  uuid: ^4.5.2
  xml: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of the package.
  flutter_lints: ^6.0.0
  build_runner: ^2.4.11
  flutter_launcher_icons: ^0.14.4

# flutter_launcher_icons configuration
flutter_launcher_icons:
  android: true
  ios: false
  min_sdk_android: 21
  image_path_android: assets/images/logo.png
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: assets/images/logo.png
  adaptive_icon_monochrome: assets/images/icon_mono.png

# For information on the generic Dart part of this file, see the following page:
# https://dart.dev/tools/pubspec

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/db/irblaster.sqlite
    - assets/images/logo.png
    - assets/ON.png
    - assets/OFF.png
    - assets/UP.png
    - assets/DOWN.png
    - assets/STROBE.png
    - assets/FLASH.png
    - assets/SMOOTH.png
    - assets/COOL.png
    - assets/BLUE.png
    - assets/BLUE0.png
    - assets/BLUE1.png
    - assets/BLUE2.png
    - assets/BLUE3.png
    - assets/RED.png
    - assets/RED0.png
    - assets/RED1.png
    - assets/RED2.png
    - assets/RED3.png
    - assets/GREEN.png
    - assets/GREEN0.png
    - assets/GREEN1.png
    - assets/GREEN2.png
    - assets/GREEN3.png
    - assets/WARM.png
    - assets/1h.png
    - assets/2h.png
    - assets/4h.png
    - assets/6h.png
